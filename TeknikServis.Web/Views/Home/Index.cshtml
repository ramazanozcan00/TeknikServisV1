@model TeknikServis.Web.Models.DashboardViewModel
@{
    ViewData["Title"] = "Genel Bakış";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 fw-bold text-dark mb-1">Genel Bakış</h1>
        <p class="text-muted small mb-0">Teknik servis operasyonlarınızın anlık özeti.</p>
    </div>
    <div class="d-flex gap-2">
        <a href="/ServiceTicket/Create" class="btn btn-primary shadow-sm">
            <i class="fa-solid fa-plus me-2"></i>Yeni Kayıt Aç
        </a>
    </div>
</div>

<div class="row g-3 mb-4">
    <div class="col-12 col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100 overflow-hidden">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <span class="text-uppercase text-muted small fw-bold">Aktif Servisler</span>
                        <h2 class="mt-2 mb-0 fw-bold text-primary">@Model.ActiveTickets</h2>
                    </div>
                    <div class="bg-primary bg-opacity-10 p-3 rounded-circle text-primary">
                        <i class="fa-solid fa-screwdriver-wrench fa-xl"></i>
                    </div>
                </div>
                <div class="mt-3 small">
                    <span class="text-danger fw-bold"><i class="fa-solid fa-clock me-1"></i>@Model.PendingRepairs</span>
                    <span class="text-muted">acil işlem bekliyor</span>
                </div>
            </div>
        </div>
    </div>

    <div class="col-12 col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <span class="text-uppercase text-muted small fw-bold">Kayıtlı Müşteri</span>
                        <h2 class="mt-2 mb-0 fw-bold text-dark">@Model.TotalCustomers</h2>
                    </div>
                    <div class="bg-success bg-opacity-10 p-3 rounded-circle text-success">
                        <i class="fa-solid fa-users fa-xl"></i>
                    </div>
                </div>
                <div class="mt-3 small">
                    <span class="text-success fw-bold"><i class="fa-solid fa-arrow-trend-up me-1"></i>%12</span>
                    <span class="text-muted">geçen aya göre artış</span>
                </div>
            </div>
        </div>
    </div>

    <div class="col-12 col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <span class="text-uppercase text-muted small fw-bold">Bu Ay Ciro</span>
                        <h2 class="mt-2 mb-0 fw-bold text-dark">₺@Model.MonthlyRevenue.ToString("N0")</h2>
                    </div>
                    <div class="bg-info bg-opacity-10 p-3 rounded-circle text-info">
                        <i class="fa-solid fa-wallet fa-xl"></i>
                    </div>
                </div>
                <div class="mt-3 small">
                    <span class="text-muted">Toplam Ciro: </span>
                    <span class="fw-bold text-dark">₺@Model.TotalRevenue.ToString("N0")</span>
                </div>
            </div>
        </div>
    </div>

    <div class="col-12 col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <span class="text-uppercase text-muted small fw-bold">Kritik Stok</span>
                        <h2 class="mt-2 mb-0 fw-bold text-danger">@Model.LowStockCount</h2>
                    </div>
                    <div class="bg-danger bg-opacity-10 p-3 rounded-circle text-danger">
                        <i class="fa-solid fa-boxes-stacked fa-xl"></i>
                    </div>
                </div>
                <div class="mt-3 small">
                    <a href="/SparePart/Index" class="text-decoration-none small text-danger fw-semibold">
                        Stokları İncele <i class="fa-solid fa-arrow-right ms-1"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row g-3 mb-4">

    <div class="col-lg-8">
        <div class="card border-0 shadow-sm h-100">
            <div class="card-header bg-white border-bottom py-3 d-flex justify-content-between align-items-center">
                <h5 class="card-title fw-bold mb-0 text-danger">
                    <i class="fa-solid fa-stopwatch me-2"></i>Acil / Bekleyen İşler
                </h5>
                <a href="/ServiceTicket/Index?status=Bekliyor" class="btn btn-sm btn-outline-danger">
                    Hepsini Gör <i class="fa-solid fa-chevron-right ms-1"></i>
                </a>
            </div>
            <div class="table-responsive">
                <table class="table table-hover table-nowrap mb-0 align-middle">
                    <thead class="bg-light text-muted small text-uppercase">
                        <tr>
                            <th class="ps-4">Fiş No</th>
                            <th>Müşteri</th>
                            <th>Cihaz</th>
                            <th>Durum</th>
                            <th>Bekleme</th>
                            <th class="text-end pe-4">İşlem</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.UrgentTickets != null && Model.UrgentTickets.Any())
                        {
                            @foreach (var item in Model.UrgentTickets)
                            {
                                // Bekleme süresi hesabı
                                var timeSpan = DateTime.Now - item.CreatedDate;
                                string waitingTime;
                                string badgeColor;

                                if (timeSpan.TotalDays >= 1)
                                {
                                    waitingTime = $"{(int)timeSpan.TotalDays} Gün";
                                    badgeColor = timeSpan.TotalDays > 7 ? "bg-danger" : "bg-warning text-dark";
                                }
                                else
                                {
                                    waitingTime = $"{(int)timeSpan.TotalHours} Saat";
                                    badgeColor = "bg-info text-dark";
                                }

                                // Satıra tıklayınca detaya gitmesi için onclick
                                <tr onclick="location.href='@Url.Action("Details", "ServiceTicket", new { id = item.Id })'" style="cursor: pointer;">

                                    <td class="ps-4">
                                        <span class="fw-bold text-primary">
                                            <i class="fa-solid fa-ticket me-1 text-muted opacity-50"></i>@item.FisNo
                                        </span>
                                    </td>
                                    <td>
                                        @if (item.Customer != null)
                                        {
                                            <div class="d-flex align-items-center">
                                                <div class="ms-0">
                                                    <h6 class="mb-0 fw-semibold text-dark" style="font-size: 0.9rem;">
                                                        @item.Customer.FirstName @item.Customer.LastName
                                                        @if (!string.IsNullOrEmpty(item.Customer.CompanyName))
                                                        {
                                                            <span class="text-muted small fw-normal"> - @item.Customer.CompanyName</span>
                                                        }
                                                    </h6>
                                                    <small class="text-muted"><i class="fa-solid fa-phone me-1" style="font-size: 0.7rem;"></i>@item.Customer.Phone</small>
                                                </div>
                                            </div>
                                        }
                                        else
                                        {
                                            <span>-</span>
                                        }
                                    </td>
                                    <td>
                                        <span class="text-dark">@(item.DeviceBrand != null ? item.DeviceBrand.Name : "") @item.DeviceModel</span>
                                    </td>
                                    <td>
                                        @{
                                            // DURUM RENGİ AYARLAMASI
                                            var statusColor = item.Status switch
                                            {
                                                "Yeni" => "bg-info text-dark",
                                                "Bekliyor" => "bg-warning text-dark",
                                                "İşlem Bekliyor" => "bg-warning text-dark",
                                                "Parça Bekliyor" => "bg-danger",
                                                "İşlemde" => "bg-primary",
                                                "Onarım Sürüyor" => "bg-primary",
                                                "Onarım Bekliyor" => "bg-warning text-dark",
                                                "Tamamlandı" => "bg-success",
                                                "Onarım Tamamlandı" => "bg-success",
                                                _ => "bg-secondary"
                                            };
                                        }
                                        <span class="badge @statusColor bg-opacity-75 rounded-pill px-2">@item.Status</span>
                                    </td>
                                    <td>
                                        <span class="badge @badgeColor rounded-pill font-monospace">
                                            <i class="fa-regular fa-clock me-1"></i>@waitingTime
                                        </span>
                                    </td>
                                    <td class="text-end pe-4">
                                        <a href="@Url.Action("Details", "ServiceTicket", new { id = item.Id })" class="btn btn-sm btn-light text-primary">
                                            Detay <i class="fa-solid fa-arrow-right ms-1"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="6" class="text-center py-5 text-muted">
                                    <i class="fa-regular fa-thumbs-up fa-2x mb-2 text-success opacity-50"></i>
                                    <br />
                                    Harika! Bekleyen acil iş bulunmamaktadır.
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card border-0 shadow-sm mb-4">
            <div class="card-header bg-white border-bottom-0 py-3">
                <h5 class="card-title fw-bold mb-0 text-dark">Servis Durumları</h5>
            </div>
            <div class="card-body d-flex align-items-center justify-content-center position-relative">
                <canvas id="statusChart" style="max-height: 250px;"></canvas>
            </div>
            <div class="card-footer bg-white border-0 text-center pb-4">
                <small class="text-muted">Toplam @Model.ActiveTickets aktif kayıt</small>
            </div>
        </div>

        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white border-bottom py-3">
                <h5 class="card-title fw-bold mb-0 text-dark">Canlı Döviz Kurları (TCMB)</h5>
            </div>
            <div class="card-body p-0">
                <table class="table table-hover align-middle mb-0">
                    <thead class="bg-light text-muted small text-uppercase">
                        <tr>
                            <th class="ps-3">Kod</th>
                            <th>Alış</th>
                            <th>Satış</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (ViewBag.Currencies != null)
                        {
                            foreach (var item in (List<TeknikServis.Core.DTOs.CurrencyDto>)ViewBag.Currencies)
                            {
                                // Sadece USD, EUR ve GBP gösterimi
                                if (item.Code == "USD" || item.Code == "EUR" || item.Code == "GBP")
                                {
                                    <tr>
                                        <td class="ps-3 fw-bold text-primary">@item.Code</td>
                                        <td>@item.ForexBuying</td>
                                        <td>@item.ForexSelling</td>
                                    </tr>
                                }
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="3" class="text-center p-3 text-muted">Kur bilgisi alınamadı.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-header bg-white border-bottom py-3 d-flex justify-content-between align-items-center">
        <h5 class="card-title fw-bold mb-0 text-dark">Son Servis Kayıtları</h5>
        <a href="/ServiceTicket/Index" class="btn btn-sm btn-light text-primary fw-semibold">Tümünü Gör</a>
    </div>
    <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
            <thead class="bg-light text-muted small text-uppercase">
                <tr>
                    <th class="ps-4">Servis No</th>
                    <th>Müşteri</th>
                    <th>Cihaz</th>
                    <th>Durum</th>
                    <th>Tarih</th>
                    <th class="text-end pe-4">İşlem</th>
                </tr>
            </thead>
            <tbody class="border-top-0">
                @if (Model.RecentTickets != null && Model.RecentTickets.Any())
                {
                    @foreach (var item in Model.RecentTickets)
                    {
                        <tr>
                            <td class="ps-4 fw-bold text-primary">#@item.FisNo</td>
                            <td>
                                <div class="d-flex align-items-center">
                                    <div class="avatar-initials bg-light text-primary rounded-circle me-2 d-flex align-items-center justify-content-center" style="width:32px; height:32px; font-size: 12px; font-weight:bold;">
                                        @(item.Customer?.FirstName?.Substring(0, 1) ?? "M")
                                    </div>
                                    <div>
                                        <div class="fw-semibold text-dark">@item.Customer?.FirstName @item.Customer?.LastName</div>
                                        <div class="small text-muted">@item.Customer?.Phone</div>
                                    </div>
                                </div>
                            </td>
                            <td>@(item.DeviceBrand?.Name ?? "-") @item.DeviceModel</td>
                            <td>
                                @{
                                    var statusClass = item.Status switch
                                    {
                                        "Tamamlandı" => "bg-success text-success",
                                        "İşlemde" => "bg-primary text-primary",
                                        "Bekliyor" => "bg-warning text-warning",
                                        "İptal" => "bg-danger text-danger",
                                        _ => "bg-secondary text-secondary"
                                    };
                                }
                                <span class="badge @statusClass bg-opacity-10 px-2 py-1 rounded-pill">@item.Status</span>
                            </td>
                            <td class="text-muted small">@item.CreatedDate.ToString("dd.MM.yyyy")</td>
                            <td class="text-end pe-4">
                                <a href="/ServiceTicket/Details/@item.Id" class="btn btn-sm btn-icon btn-light text-muted" title="Detay">
                                    <i class="fa-solid fa-eye"></i>
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="6" class="text-center py-5 text-muted">
                            <i class="fa-solid fa-inbox fa-2x mb-3 d-block opacity-25"></i>
                            Henüz kayıt bulunmuyor.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        // SADECE DURUM GRAFİĞİ
        const ctxStatus = document.getElementById('statusChart').getContext('2d');
        new Chart(ctxStatus, {
            type: 'doughnut',
            data: {
                labels: [@Html.Raw(Model.TicketStatusLabels)],
                datasets: [{
                    data: [@Model.TicketStatusCounts],
                    backgroundColor: ['#fbbf24', '#2563eb', '#ef4444', '#10b981', '#6b7280'],
                    borderWidth: 0,
                    hoverOffset: 4
                }]
            },
            options: {
                responsive: true,
                cutout: '75%',
                plugins: {
                    legend: { position: 'bottom', labels: { usePointStyle: true, padding: 20 } }
                }
            }
        });
    </script>
}